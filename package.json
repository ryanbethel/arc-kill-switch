{
  "name": "arc-macro-dns",
  "version": "1.0.0",
  "description": "- must be deployed in us-east-1 - must be in the same account as the",
  "main": "src/macros/dns/index.js",
  "scripts": {
    "lint": "eslint . --fix"
  },
  "author": "Brian LeRoux <b@brian.io>",
  "dependencies": {
    "@architect/architect": "^9.0.0",
    "@architect/destroy": "^2.0.1",
    "@architect/functions": "^3.13.3",
    "extract-domain": "^2.2.1"
  },
  "devDependencies": {
    "@architect/eslint-config": "^1.0.0",
    "eslint": "^7.11.0"
  },
  "eslintConfig": {
    "rules": {
      "require-await": 0
    },
    "extends": "@architect/eslint-config"
  }
}
